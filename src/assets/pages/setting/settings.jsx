import Sidebar from '../../components/Sidebar';

function Settings() {
    return (
        <div>
            <Sidebar />
            <h1 style={{ position: 'absolute', top: 0, left: 80 }}>There is nothing</h1>
        </div>
    );
}

export default Settings;